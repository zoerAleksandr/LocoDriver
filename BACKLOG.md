# BACKLOG.md

Задачи, которые отложены за пределами текущего релизного цикла
(см. `60_IOS_TODO.md` — план релиза iOS).

## Из задачи 1.4 (сетевая надёжность)

### Auto-logout при 401 Unauthorized

**Контекст**: сейчас при 401 показываем alert «Нужна повторная авторизация» —
пользователь сам идёт в Profile → Logout → Login. Auto-logout (немедленная
очистка токена и переход на Login-экран) рассматривался, но отложен:

- Риск: временная 401 от сервера (баг, проблема с прокси) разлогинит всех.
- Нужна аккуратная стратегия: auto-logout только после N подряд 401 на разные
  эндпоинты, либо явный признак от сервера (`token_expired: true` в response).

**Когда делать**: после релиза iOS, когда соберём метрику частоты 401 в проде
(через Sentry).

### Полный формат retry-лога: `attempt=N delay=Xms cause=ClassName`

**Контекст**: в Ktor `HttpRequestRetry` хуки разделены — `retryOnExceptionIf`
видит cause без attempt, `modifyRequest` видит attempt без cause. Сейчас
пишем два раздельных лога. Полный формат требует thread-safe общего state
(например ConcurrentHashMap по request id) и нетривиальной координации.

**Когда делать**: если в продакшене будем активно дебажить retry-логику и
двух раздельных логов перестанет хватать.

## Из задачи 1.4 — деплой и UX

### DeepLink приём не работает: «Not found» при открытии общей ссылки

**Контекст**: отправка через «Поделиться» в приложении работает корректно
(POST /v1/share/route/ генерирует ссылку). Но получатель открывает 
`locodriver://share/<UUID>` → alert «Not found». GET /v1/share/route/{UUID}
возвращает 404.

**Возможные причины**:
- Endpoint несоответствие (URL в KtorRemoteRestApi vs реальный сервер)
- Парсер deep-link в SharedRouteLinkHandler.handle() неправильно извлекает
  ID из URL (Universal Link `https://locodriver.ru/r/<id>` vs custom-scheme
  `locodriver://share/<id>`)
- Серверная сторона не сохраняет shared-ссылки или TTL короткий

**Тест для разбора**:
1. Получить ссылку через "Поделиться" в приложении
2. `curl http://87.228.110.32:8766/v1/share/route/<ID>` — увидеть 200 или 404
3. Сравнить parsed ID в SharedRouteLinkHandler с тем что в URL

**Приоритет**: средний. Не блокер релиза (ссылки используются редко).
**Когда делать**: после задач 1.1 (CMP cleanup) и 1.2 (memory leaks).
