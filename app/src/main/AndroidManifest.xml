<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".StartApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocoDriver"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.LocoDriver.Splash">

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="open"
                    tools:ignore="AppLinkUrlError" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="robokassa" />
                <data android:host="open" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"/>
                <data android:scheme="https"/>
                <data android:host="ipol.ru"/>
                <data android:host="*.ipol.ru"/>
                <data android:pathPattern="webService/*"
                    tools:ignore="AppLinkUrlError" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="z_company_loco_driver_scheme" />
            </intent-filter>

            <!-- Публичные ссылки на маршруты — основной HTTPS-формат:
                 https://locodriver.ru/r/{id}
                 autoVerify=true → Android при первом запуске проверяет
                 /.well-known/assetlinks.json на домене locodriver.ru и регистрирует
                 App Link, чтобы такие ссылки открывались напрямую в приложении,
                 минуя браузер и диалог "Open with…". -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="locodriver.ru"
                      android:pathPrefix="/r/" />
            </intent-filter>

            <!-- Fallback: кастомная схема locodriver://share/{id}.
                 Используется как резерв (например, со старых ссылок и для iOS,
                 где Universal Links не настроены — нет Apple Developer Program). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="locodriver" android:host="share" />
            </intent-filter>

            <!-- Открытие .zroute файлов с кастомным MIME (прямой шаринг из приложения) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content"
                    android:mimeType="application/vnd.com.z_company.loco_driver.route" />
                <data android:scheme="file"
                    android:mimeType="application/vnd.com.z_company.loco_driver.route" />
            </intent-filter>

            <!-- Открытие .zroute файлов через Telegram и другие мессенджеры (отправляют как octet-stream) -->
            <!-- MainActivity проверяет расширение файла через ContentResolver.DISPLAY_NAME -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="application/octet-stream" />
                <data android:scheme="file" android:mimeType="application/octet-stream" />
            </intent-filter>

        </activity>

        <!-- FileProvider для прикрепления файлов к email (отчеты об ошибках синхронизации) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.z_company.loco_driver.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Glance Widget (full) -->
        <receiver
            android:name=".widget.LocoDriverWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <!-- Обновляем PendingIntent-ы после обновления APK — иначе старые интенты
                     не содержат EXTRA_CALLBACK_CLASS и ActionTrampolineActivity крашится -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/loco_driver_widget_info" />
        </receiver>

        <!-- Glance Widget (small — time/norm only) -->
        <receiver
            android:name=".widget.LocoDriverSmallWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/loco_driver_small_widget_info" />
        </receiver>

    </application>
</manifest>